# 1. Clone the repo you just created
git clone https://github.com/soham53crtl/prom.git
cd prom
# 2. Add and push everything
git add .
git commit -m "Add EventMind smart event assistant"
git push origin main
